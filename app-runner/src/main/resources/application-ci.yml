spring:
  datasource:
    url: jdbc:postgresql://postgres:5432/oremus
    username: postgres
    password: postgres

  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: create